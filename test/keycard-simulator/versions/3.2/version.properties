# Applet generation 3.2 — SecureChannel v1 / classic password pairing (INS_PAIR 0x12).
# Matches keycard-qt df00b931 (the lib status-keycard-qt currently pins). This is the DEFAULT version.
appletVersion=3.2
mainClass=im.status.keycardqt.sim.v32.Main32
# Card recipe is install-only (blank / post-FactoryReset); no SDK or BouncyCastle needed.
needsPersonalization=false

# Provenance (set/printed by refresh-artifacts.sh; libs are committed):
statusKeycardSha=72e9574
jcardsimSha=e1e351a
# Host SDK is NOT used by the 3.2 (install-only) recipe; recorded for completeness:
statusKeycardJavaSdk=com.github.status-im.status-keycard-java:desktop:e898afd
